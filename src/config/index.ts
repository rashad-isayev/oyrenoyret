/**
 * Configuration & Constants
 * 
 * Centralized configuration values, constants, and environment-based settings.
 * All configuration should be accessed through this module to ensure consistency.
 * 
 * Security Note: Never expose sensitive values to the client.
 */

export * from './credits';
