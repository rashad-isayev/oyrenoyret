export const MAX_IMAGE_UPLOAD_BYTES = 4 * 1024 * 1024; // 4MB

// Max number of images allowed in a single discussion post or reply.
export const MAX_DISCUSSION_IMAGES = 2;

// Problem sprint submissions: limit attachments to keep moderation + storage manageable.
export const MAX_SPRINT_SUBMISSION_IMAGES_PER_ANSWER = 3;
export const MAX_SPRINT_SUBMISSION_IMAGES_TOTAL = 10;
